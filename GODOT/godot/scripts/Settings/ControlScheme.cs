namespace LifeLike.Game.Settings;

/// <summary>Sterowanie dotykiem: gesty na mapie + pasek akcji albo wirtualna gałka (+ ten sam pasek).</summary>
public enum ControlScheme
{
    Swipe = 0,
    Joystick = 1,
}
